# 3. อาร์เรย์

อ้างอิงจากหนังสือ หน้า 339-349

| ตัวอย่างที่ (หนังสือ) | หน้า | คำอธิบาย | ไฟล์ |
|---|---|---|---|
| 21 | 340 | การสร้างอาร์เรย์ด้วยเครื่องหมายวงเล็บสี่เหลี่ยม | example-21-array-literal.js |
| 22 | 341 | การสร้างอาร์เรย์ด้วย Array Constructor | example-22-array-constructor.js |
| 23 | 341 | การเข้าถึงข้อมูลในอาร์เรย์ | example-23-accessing-array-data.js |
| 24 | 342-343 | การเปลี่ยนแปลงข้อมูลในอาร์เรย์ | example-24-changing-array-data.js |
| 25 | 345 | เมธอด push() สำหรับเพิ่มข้อมูลท้ายอาร์เรย์ | example-25-push-method.js |
| 26 | 346 | เมธอด pop() สำหรับลบข้อมูลท้ายอาร์เรย์ | example-26-pop-method.js |
| 27 | 346-347 | เมธอด unshift() สำหรับเพิ่มข้อมูลต้นอาร์เรย์ | example-27-unshift-method.js |
| 28 | 347-348 | เมธอด shift() สำหรับลบข้อมูลต้นอาร์เรย์ | example-28-shift-method.js |
| 29 | 348-349 | คุณสมบัติ length | example-29-length-property.js |

หมายเหตุ: ในหัวข้อ "การลบข้อมูลต้นอาร์เรย์ด้วย shift()" (หน้า 347) กล่องรูปแบบ (รูปแบบ) ของหนังสือพิมพ์คำสั่งเป็น `ชื่ออาร์เรย์.unshift(ข้อมูล_1, ข้อมูล_2, ..., ข้อมูล_n);` ซึ่งเป็นชื่อเมธอดที่ไม่ตรงกับหัวข้อ (ควรเป็น shift()) แต่ตัวอย่างที่ 28 ในเนื้อหาใช้เมธอด shift() ถูกต้อง จึงคงข้อสังเกตนี้ไว้เป็นหมายเหตุโดยไม่มีการแก้ไขโค้ดตัวอย่าง
